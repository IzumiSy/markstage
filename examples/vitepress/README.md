# example-vitepress

Example project using VitePress with `@izumisy/vitepress-plugin-react-preview`.

## Commands

```bash
pnpm dev       # Start VitePress dev server
pnpm build     # Build the VitePress site
pnpm preview   # Preview the production build
```
